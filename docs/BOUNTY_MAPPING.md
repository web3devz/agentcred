# Bounty Mapping (Draft)

- EigenCompute: `apps/verifier-tee` Docker deploy + `/verify`
- OpenServ: `packages/agents-sdk` + `apps/worker/orchestrators/openserv.ts`
- Arkhai: escrow-compatible patterns in `contracts/src/EscrowAgreement.sol`
- Octant: transparent scoring + receipts in API/docs
- ampersend: payout rail client in `apps/api/src/clients/ampersend.ts`
- Status: gasless flow adapter in `apps/api/src/clients/status.ts`
- Protocol Labs ERC-8004: receipt + identity compatibility
- Base: deploy contracts on Base Sepolia + service endpoints
